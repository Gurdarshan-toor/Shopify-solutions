{% style %}
  #shopify-section-{{ section.id }} {
    background-color: {{ section.settings.sectionBgClr }};
    color:{{ section.settings.color_accent }};
  }
  #shopify-section-{{ section.id }} .product-item__inner:hover .product-item__meta {
    color: {{ section.settings.color_accent_hover }};
  }
{% endstyle %}
